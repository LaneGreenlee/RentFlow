{
  "name": "rentflow-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n api,client -c blue,green \"mvn spring-boot:run\" \"npm --prefix client run dev\"",
    "dev:client": "npm --prefix client run dev",
    "dev:api": "mvn spring-boot:run"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
